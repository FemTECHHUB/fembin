__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.env
.env.*
!.env.example
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.db
*.db-shm
*.db-wal
.DS_Store

# cron job output on a deployed server (DEPLOYMENT.md step 9) — never generated locally
logs/*.log

# cPanel Passenger entry point — the host generates/owns the deployed copy. Untracked so
# a git pull can never conflict with, overwrite, or be overwritten by the server's version.
passenger_wsgi.py
