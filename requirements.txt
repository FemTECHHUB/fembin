# Production dependencies for cPanel (Passenger/WSGI) deployment.
#
# Base-generated from uv.lock via:
#   uv export --format requirements.txt --no-dev --no-hashes --no-emit-project
# then hand-curated to drop uvicorn and its "standard" extras (uvloop, httptools,
# watchfiles, websockets, click, colorama, pyyaml) — Passenger is the app server in
# this deployment, it never invokes uvicorn, and several of those are C/Rust extensions
# that add real install risk on shared hosting for zero runtime benefit. `uvicorn` stays
# a normal dependency in pyproject.toml for local dev (`uv run uvicorn ...`); this file
# is the cPanel-specific trim of that. Regenerate the base list after any dependency
# change, then re-apply this same trim.
a2wsgi==1.10.10
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
bcrypt==5.0.0
certifi==2026.7.22
fastapi==0.141.1
greenlet==3.5.5 ; platform_machine == 'AMD64' or platform_machine == 'WIN32' or platform_machine == 'aarch64' or platform_machine == 'amd64' or platform_machine == 'ppc64le' or platform_machine == 'win32' or platform_machine == 'x86_64'
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
mako==1.4.1
markupsafe==3.0.3
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.15.0
pyjwt==2.13.0
pymysql==1.2.0
python-dotenv==1.2.2
sqlalchemy==2.0.52
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
